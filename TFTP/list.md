
string 字符串的操作
线程体
静态函数或变量
友元函数
智能指针

把类本身的指针this传递给线程体函数，从而实现在函数中使用类的函数

静态成员为了防止重复定义，一般要类内声明并类外定义。在c++ 17中可以使用inline内联函数，直接在类的内部声明加定义

inline内联函数

c++ 文件的io操作

使用string字符串封装报文，怎么read读到字符串中
以及怎么使用函数转变为网络字节序，组装报文

转变为网络字节序并封装报文时，有两种思想
一种是直接把一个数字拆开或者是把两个字节拼接为一个元素
或者是使用uint16_t 然后再转变内存的角度，直接一下拼上去

reinterpret_cast

真正理解为什么最好先把每个字节先转为uint8_t再移动，最后再转化为16

记住s.c_str() 是c风格字符串，并且牢记收缩缓冲区string的长度

并且记住用c风格字符串构造字符串时，0导致的数据截断